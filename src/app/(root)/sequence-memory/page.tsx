import { SequenceMemoryContent } from "@/features/sequence-memory/SequenceMemoryContent";

export const metadata = {
  title: "Sequence Memory | APT-PWK",
  description: "Test your ability to remember and recall sequences of colors.",
  keywords: ["sequence", "memory", "pwk-apt", "aptitude", "exercises"],
};

export default function SequenceMemoryPage() {
  return <SequenceMemoryContent />;
}
